DATABASE_URL=postgres://postgres:password@localhost:5432/con_store
SESSION_SECRET=your-very-secret-string
# On each request, Express uses the SESSION_SECRET to verify that the cookie hasn’t been altered
# If the signature doesn’t match, the session is rejected
PORT=3000